# دليل بناء تطبيق أندرويد (APK) لمنصة ERP الموحدة عبر GitHub

هذا مشروع أندرويد كامل وجاهز، ومعه **إعداد آلي (GitHub Actions)** يبني لك ملف
APK حقيقي وجاهز تلقائيًا على خوادم GitHub — **بدون أي تثبيت برامج على جهازك
إطلاقًا** (لا تحتاج Android Studio ولا أي أداة أخرى).

---

## الطريقة 1 (الأسهل): البناء التلقائي عبر GitHub — بدون تثبيت أي شيء

### الخطوة 1: أنشئ حسابًا ومستودعًا (Repository) على GitHub

- اذهب إلى github.com وأنشئ حسابًا مجانيًا إن لم يكن لديك
- اضغط زر **New** لإنشاء مستودع جديد (Repository)
- اختر اسمًا (مثلاً `erp-android-app`) واجعله **Public** أو **Private** كما تحب
- لا تُفعّل خيار "Add a README file" حتى يبقى المستودع فارغًا تمامًا

### الخطوة 2: ارفع محتويات مجلد ErpApp إلى المستودع

أسهل طريقة بدون أوامر برمجية:
- افتح المستودع الذي أنشأته على الموقع
- اضغط **Add file → Upload files**
- اسحب **كل محتويات** مجلد `ErpApp` (وليس المجلد نفسه) وأفلتها هناك
  (تأكد أن مجلد `.github` مرفوع أيضًا — هو مخفي فقد يحتاج سحبه يدويًا أو رفعه
  عبر السحب المباشر من مستكشف الملفات)
- اضغط **Commit changes** في الأسفل

### الخطوة 3: راقب البناء التلقائي

- من داخل المستودع، اضغط تبويب **Actions** في الأعلى
- ستجد عملية بناء باسم "بناء تطبيق أندرويد (APK)" تعمل تلقائيًا (دائرة صفراء
  متحركة) — انتظر 3 إلى 6 دقائق تقريبًا حتى تتحول لعلامة **صح خضراء** ✅

### الخطوة 4: نزّل ملف APK الجاهز

- اضغط على عملية البناء المكتملة (✅)
- انزل لأسفل الصفحة إلى قسم **Artifacts**
- اضغط على `erp-app-debug-apk` لتنزيله (سيأتي كملف مضغوط صغير يحتوي APK بداخله)
- فك الضغط، وستجد ملف **app-debug.apk** جاهزًا 100%

انقله لموبايلك وثبّته (نفس خطوات التثبيت والاستخدام أدناه).

> إذا فشلت العملية (علامة ❌ حمراء)، اضغط عليها لقراءة سبب الخطأ بالتفصيل،
> وأرسل لي نص الخطأ وسأساعدك في حله فورًا.

---

## الطريقة 2 (بديلة): البناء يدويًا عبر Android Studio على جهازك



- حمّله مجانًا من: https://developer.android.com/studio
- ثبّته بالإعدادات الافتراضية (سيحمّل تلقائيًا كل ما يحتاجه من أدوات).

## الخطوة 2: افتح المشروع

- افتح Android Studio
- اختر **Open** ثم اختر مجلد `ErpApp` (المرفق معك)
- انتظر حتى ينتهي من "Gradle Sync" (شريط تحميل أسفل الشاشة) — قد يأخذ بضع دقائق
  في أول مرة فقط لأنه يحمّل أدوات البناء

> لو ظهرت رسالة تسأل عن "Gradle wrapper" أو تحديث إصدار، اضغط **OK / Yes**
> دائمًا — Android Studio يتكفّل بكل شيء تلقائيًا.

## الخطوة 3: أنتج ملف APK (هذه هي "نقرة الزر")

من القائمة العلوية:

```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

بعد انتهاء البناء، ستظهر رسالة صغيرة أسفل يمين الشاشة تقول "APK(s) generated
successfully" مع رابط **locate** — اضغط عليه ليفتح لك مجلد يحتوي ملف:

```
app-debug.apk
```

هذا هو ملف APK الجاهز للتثبيت.

## الخطوة 4: انقله وثبّته على الموبايل

- انسخ `app-debug.apk` إلى الموبايل (عبر كابل USB، بلوتوث، أو رفعه على
  Google Drive وتنزيله من الموبايل)
- افتح الملف على الموبايل واضغط **تثبيت**
- إن ظهرت رسالة "مصدر غير معروف" (Unknown Source)، اسمح بالتثبيت من هذا
  المصدر مرة واحدة — هذا طبيعي لأي تطبيق لم يُنزَّل من متجر Google Play

## الخطوة 5: أول تشغيل — أدخل رابط الخادم

عند فتح التطبيق أول مرة، سيطلب منك **رابط خادم البرنامج**:

- لو تستخدم اللابتوب كخادم على نفس شبكة الواي فاي: اكتب `http://192.168.x.x:4000`
  (نفس الرابط الذي يظهر عند تشغيل `start.bat` على اللابتوب)
- لو تستخدم خادم سحابي: اكتب رابط الدومين أو IP الخادم (مثل `https://erp.شركتك.com`)

يمكنك تغيير هذا الرابط لاحقًا في أي وقت من: **القائمة (⋮) → تغيير رابط الخادم**

---

## أسئلة متكررة

**هل التطبيق يخزّن أي بيانات داخل الموبايل؟**
لا، كل البيانات تبقى في الخادم فقط (اللابتوب أو الخادم السحابي). التطبيق هو
مجرد نافذة تعرض نفس واجهة البرنامج بشكل تطبيق مستقل بدل المتصفح.

**هل أقدر أنشره على Google Play لاحقًا؟**
نعم من ناحية تقنية، لكن يتطلب: حساب مطوّر Google Play (رسوم اشتراك لمرة
واحدة 25$)، وبناء نسخة "Release" موقّعة رقميًا بدل "Debug"، ومراجعة Google
للتطبيق. هذا اختياري تمامًا — التثبيت المباشر (Sideload) بدون متجر يعمل
بشكل كامل وطبيعي كما هو موضح أعلاه.

**التطبيق يظهر خطأ "تعذّر الاتصال بالخادم"؟**
تأكد أن: (1) الخادم (اللابتوب أو الخادم السحابي) شغّال فعليًا، (2) الموبايل
على نفس شبكة الواي فاي لو تستخدم اللابتوب، (3) الرابط المدخل صحيح تمامًا
بدون أخطاء إملائية.
